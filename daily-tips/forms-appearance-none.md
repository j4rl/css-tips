# Remove native appearance
Reset control styles for custom UI.

```css
.select {
  appearance: none;
  background: #fff;
  border: 1px solid #cbd5e1;
  padding: 0.5rem 2rem 0.5rem 0.75rem;
}
```

Why it helps: consistent styling across browsers.
