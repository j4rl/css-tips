# UL/LI menu
Use a list for nav and reset the default styles.

```css
.menu {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  gap: 1rem;
}
```

Why it helps: semantic nav with clean spacing.
