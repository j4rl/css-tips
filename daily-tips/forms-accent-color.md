# Accent color
Style native checkboxes and radios without custom markup.

```css
input[type="checkbox"],
input[type="radio"] {
  accent-color: #2563eb;
}
```

Why it helps: consistent theming with native controls.
